import { LandingPagePipe } from './landing-page.pipe';

describe('LandingPagePipe', () => {
  it('create an instance', () => {
    const pipe = new LandingPagePipe();
    expect(pipe).toBeTruthy();
  });
});
