import { RegistrationViewModule } from './registration-view-module';

describe('RegistrationViewModule', () => {
  it('should create an instance', () => {
    expect(new RegistrationViewModule()).toBeTruthy();
  });
});
